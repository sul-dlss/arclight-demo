dist: xenial
sudo: false
language: ruby
notifications:
  email: false
rvm:
  - 2.6.6
  - 2.7.1

jdk:
  - openjdk11
cache: bundler

before_script:
  - find data/ead -name '*.xml' | xargs xmllint --noout --schema data/xsd/ead.xsd || (exit 0)
addons:
  apt:
    packages:
    - libxml2-utils
